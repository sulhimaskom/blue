// Mock for @clerk/backend
module.exports = {
  // Add any required exports here
};

// Add dummy test to satisfy Jest
describe("Clerk Backend Mock", () => {
  it("should provide Clerk backend mock", () => {
    expect(true).toBe(true);
  });
});
